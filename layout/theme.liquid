<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    {% # Inlined CSS Variables %}
    {% render 'css-variables' %}

    {% # Load and preload the critical CSS %}
    {{ 'critical.css' | asset_url | stylesheet_tag: preload: true }}
    {{ 'theme.css' | asset_url | stylesheet_tag: preload: true }}

    {{ 'owl.carousel.min.css' | asset_url | stylesheet_tag }}
    {{ 'owl.theme.default.min.css' | asset_url | stylesheet_tag }}
    
    <script src="{{ 'jquery-3.7.1.slim.min.js' | asset_url }}" defer></script>
    <script defer src="{{ 'owl.carousel.min.js' | asset_url }}"></script>

    {% # Social, title, etc. %}
    {% render 'meta-tags' %}

    <link rel="shortcut icon" href="{{ settings.favicon_image | image_url: width: 32 }}" type="image/x-icon">

    {{ content_for_header }}
  </head>

  <body>
    {% sections 'header-group' %}

    {{ content_for_layout }}

    {% sections 'footer-group' %}
  </body>
</html>
